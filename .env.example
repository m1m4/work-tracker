# OAuth 2.0 Web application client ID from Google Cloud Console.
# This is NOT a secret - public clients ship it in the bundle by design.
# Security comes from the Authorized JavaScript origins allowlist.
VITE_GOOGLE_CLIENT_ID=
